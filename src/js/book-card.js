const image = document.querySelector('.gallery-book-img');
const popup = document.querySelector('.actions-card');
let isOpen = false;

// image.addEventListener('click', function () {
//   isOpen = !isOpen;
//   popup.style.display = isOpen ? 'block' : 'none';
// });

// document
//   .querySelector('.caterories-content')
//   .addEventListener('click', function (e) {
//     e.preventDefault();
//     const target = e.target.closest('.js-open-modal');
//     if (target) {
//       openModal(target);
//     }
//   });

// openModal();
